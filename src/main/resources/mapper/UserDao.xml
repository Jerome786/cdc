<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdc.train.dao.UserDao">

    <resultMap type="com.cdc.train.entity.User" id="UserMap">
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="lastLoginTime" column="last_login_time" jdbcType="TIMESTAMP"/>
        <result property="openId" column="open_id" jdbcType="VARCHAR"/>
        <result property="nickname" column="nickname" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
        <result property="deptId" column="dept_id" jdbcType="VARCHAR" />
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap" parameterType="String">
        select
          user_id, create_time, update_time, last_login_time, nickname, phone, email, status, dept_id
        from cdc.user
        where user_id = #{userId}
    </select>

    <!--根据openId和nickname查询单个用户-->
    <select id="queryByUser" resultMap="UserMap" parameterType="com.cdc.train.entity.User">
        SELECT
        user_id, create_time, update_time, last_login_time, nickname, phone, email, status
        FROM
        cdc.user
        WHERE
        user_id = #{user.userId}
        AND
        nickname = #{user.nickname}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          user_id, create_time, update_time, last_login_time, nickname, phone, email, status, avatar, dept_id
        from cdc.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        user_id, create_time, update_time, last_login_time, nickname, phone, email, status, dept_id
        from cdc.user
        <where>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="lastLoginTime != null">
                and last_login_time = #{lastLoginTime}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname = #{nickname}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="avatar != null and avatar != ''">
                and avatar = #{avatar}
            </if>
            <if test="dept_id != null  and dept_id != ''">
                and dept_id = #{deptId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" useGeneratedKeys="true">
        insert into cdc.user(user_id,create_time, update_time, last_login_time, nickname, phone, email, avatar, dept_id)
        values (#{userId}, #{createTime}, #{updateTime}, #{lastLoginTime}, #{nickname}, #{phone}, #{email}, #{avatar}, #{deptId})
    </insert>



    <!--通过主键修改数据-->
    <update id="update">
        update cdc.user
        <set>
            <if test="user.createTime != null">
                create_time = #{user.createTime},
            </if>
            <if test="user.updateTime != null">
                update_time = #{user.updateTime},
            </if>
            <if test="user.lastLoginTime != null">
                last_login_time = #{user.lastLoginTime},
            </if>
            <if test="user.nickname != null and user.nickname != ''">
                nickname = #{user.nickname},
            </if>
            <if test="user.phone != null and user.phone != ''">
                phone = #{user.phone},
            </if>
            <if test="user.email != null and user.email != ''">
                email = #{email},
            </if>
            <if test="user.status != null and user.status != ''">
                status = #{user.status},
            </if>
            <if test="user.avatar != null and user.avatar != ''">
                avatar = #{user.avatar},
            </if>
            <if test="user.deptId != null and user.deptId != ''">
                dept_id = #{user.deptId}
            </if>
        </set>
        where user_id = #{user.userId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from cdc.user where user_id = #{userId}
    </delete>

</mapper>